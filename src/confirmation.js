import './style.css'

class ConfirmationPage {
  constructor() {
    this.init()
  }

  init() {
    // Page initialization complete - no specific actions needed
  }
}

// Initialize confirmation page when DOM is loaded
document.addEventListener('DOMContentLoaded', () => {
  new ConfirmationPage()
})